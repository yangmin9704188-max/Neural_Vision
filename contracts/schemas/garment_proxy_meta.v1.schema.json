{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "garment_proxy_meta.v1",
  "title": "garment_proxy_meta.v1 — U1 Freeze (Garment→Fitting)",
  "description": "Minimal schema for garment_proxy_meta.json hard gate flags. Ref: unlock_conditions_u1_u2.md §2.2",
  "type": "object",
  "required": [
    "negative_face_area_flag",
    "self_intersection_flag",
    "invalid_face_flag"
  ],
  "additionalProperties": true,
  "properties": {
    "negative_face_area_flag": {
      "type": "boolean",
      "description": "True if negative face area detected (Hard Gate)"
    },
    "self_intersection_flag": {
      "type": "boolean",
      "description": "True if self intersection detected (Hard Gate)"
    },
    "invalid_face_flag": {
      "type": "boolean",
      "description": "True if degenerate/invalid faces detected (Hard Gate)"
    }
  }
}
